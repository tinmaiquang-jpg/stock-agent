# FastAPI >= 0.141 bat buoc: claude-agent-sdk keo starlette 1.x, ban fastapi cu (0.115)
# pin starlette < 0.39 va se crash khi tao APIRouter
fastapi==0.141.1
uvicorn[standard]==0.52.1

# Backend 'subscription': dung goi Claude Pro/Max qua CLAUDE_CODE_OAUTH_TOKEN.
# Package da bundle binary Claude Code nen KHONG can cai Node.js.
claude-agent-sdk==0.2.135
# Backend 'api_key': goi Messages API truc tiep
anthropic==0.40.0

python-telegram-bot==21.6

# vnstock 4.x dung API moi (vnstock.api.*); tenacity la dependency an cua no
vnstock==4.0.5
tenacity==9.1.4
pandas==2.3.3

# supabase >=2.30 moi nhan key dinh dang moi (sb_secret_...), ban 2.9 chi nhan JWT
supabase==2.31.0

apscheduler==3.10.4
jinja2==3.1.4
python-dotenv==1.0.1
pydantic-settings==2.5.2
itsdangerous==2.2.0
python-multipart==0.0.12
