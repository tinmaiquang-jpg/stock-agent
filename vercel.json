{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/**/*.py": {
      "maxDuration": 300,
      "excludeFiles": "{.venv/**,venv/**,scripts/**,migrations/**,**/__pycache__/**,*.md,Dockerfile,docker-compose.yml,Caddyfile}"
    }
  }
}
